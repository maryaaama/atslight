 mutation UpdateCompany($input: UpdateCompanyInput!) {
    updateCompany(input: $input) {
      company {
        nodeId
        id
        translations {
          nodes {
            nodeId
            name
            lang
          }
        }
        languages
        category
        companySize
        customWebsiteHost
        logoUrl
      }
    }
  }
`